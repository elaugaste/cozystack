{{- $tlsEnabled := (include "mariadb.tls.enabled" .) | eq "true" -}}
---
apiVersion: k8s.mariadb.com/v1alpha1
kind: MariaDB
metadata:
  name: {{ .Release.Name }}
spec:
  rootPasswordSecretKeyRef:
    name: {{ .Release.Name }}-credentials
    key: root

  image: "mariadb:{{ include "mariadb.versionMap" $ }}"

  port: 3306

  replicas: {{ .Values.replicas }}
  replicasAllowEvenNumber: true
  affinity:
    podAntiAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
      - labelSelector:
          matchExpressions:
          - key: app.kubernetes.io/name
            operator: In
            values:
            - mariadb
          - key: app.kubernetes.io/instance
            operator: In
            values:
            - {{ .Release.Name }}
        topologyKey: "kubernetes.io/hostname"

  replication:
    enabled: true
    #primary:
    #  podIndex: 0
    #  automaticFailover: true

  podMetadata:
    labels:
      "policy.cozystack.io/allow-to-apiserver": "true"

  metrics:
    enabled: true
    exporter:
      image: prom/mysqld-exporter:v0.15.1
      resources:
        requests:
          cpu: 50m
          memory: 64Mi
        limits:
          cpu: 300m
          memory: 512Mi
      port: 9104
    serviceMonitor:
      interval: 10s
      scrapeTimeout: 10s

  myCnfConfigMapKeyRef:
    name: {{ .Release.Name }}-my-cnf
    key: config

  service:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ $.Release.Name }}
  storage:
    size: {{ .Values.size }}
    resizeInUseVolumes: true
    waitForVolumeResize: true
    {{- with .Values.storageClass }}
    storageClassName: {{ . }}
    {{- end }}

  {{- if .Values.external }}
  primaryService:
    type: LoadBalancer
  {{- end }}

  #secondaryService:
  #  type: LoadBalancer

  {{- if $tlsEnabled }}
  {{- $tlsMap := default (dict) .Values.tls }}
  {{- $tlsRequired := index $tlsMap "required" }}
  tls:
    enabled: true
    # required: true enforces require_secure_transport=ON (strict TLS for all connections).
    # The operator manages TLS for its own internal agents (replication, health-checks).
    # Set tls.required: false only during migration when legacy clients cannot use TLS yet.
    # `kindIs "invalid"` guards against unset field; explicit false is preserved.
    required: {{ if kindIs "invalid" $tlsRequired }}true{{ else }}{{ $tlsRequired }}{{ end }}
    serverCertSecretRef:
      name: {{ .Release.Name }}-tls
    serverCASecretRef:
      name: {{ .Release.Name }}-ca-tls
  {{- end }}

  resources: {{- include "cozy-lib.resources.defaultingSanitize" (list .Values.resourcesPreset .Values.resources $) | nindent 4 }}
