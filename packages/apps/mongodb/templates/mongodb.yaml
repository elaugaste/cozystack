{{- $clusterDomain := (index .Values._cluster "cluster-domain") | default "cozy.local" }}
{{- $tlsEnabled := include "mongodb.tls.enabled" . | eq "true" -}}
{{- /*
TLS IMPLEMENTATION NOTE — operator-managed cert-manager chain
=============================================================
When TLS is enabled (spec.secrets.ssl is set), the PSMDB operator natively
integrates with cert-manager and creates the full certificate chain itself:

  1. Self-signed bootstrap Issuer
  2. CA Certificate (10-year, rotationPolicy: Never)
  3. CA-backed Issuer
  4. Leaf server+client cert with SAN list covering per-replset service
     (short, namespace, FQDN), per-pod headless DNS names, wildcards, and
     localhost.

The chart does NOT render cert-manager Issuer/Certificate resources to avoid
orphaning the operator-managed ones. Chart-rendered resources would be ignored
as trust anchors because the PSMDB operator controls the CA it embeds in pods.

KNOWN LIMITATION — external hostname SANs:
  When external: true, per-pod LoadBalancer hostnames are NOT included in the
  operator-managed leaf certificate. Injecting custom SANs requires PSMDB
  ≥ 1.22.0 (splitHorizons field). Cozystack currently ships PSMDB 1.21.1.
  Until the operator is upgraded, external TLS connections will succeed only
  if the client skips hostname verification or uses the pod's internal DNS.
  Track as a follow-up: bump PSMDB to 1.22.0 and configure splitHorizons.
*/}}
---
apiVersion: psmdb.percona.com/v1
kind: PerconaServerMongoDB
metadata:
  name: {{ .Release.Name }}
  finalizers:
    - percona.com/delete-psmdb-pods-in-order
    - percona.com/delete-psmdb-pvc
spec:
  crVersion: 1.21.1
  clusterServiceDNSSuffix: svc.{{ $clusterDomain }}
  secrets:
    users: {{ .Release.Name }}-percona-server-mongodb-users
    {{- if $tlsEnabled }}
    ssl: {{ .Release.Name }}-ssl
    {{- end }}
  pause: false
  unmanaged: false

  {{- if $tlsEnabled }}
  tls:
    # preferTLS keeps non-TLS clients working during rollout, so the
    # backup CronJob is not disrupted when TLS is first enabled.
    mode: preferTLS
  {{- end }}
  image: percona/percona-server-mongodb:{{ include "mongodb.versionMap" $ }}
  imagePullPolicy: IfNotPresent

  {{- if lt (int .Values.replicas) 3 }}
  unsafeFlags:
    replsetSize: true
  {{- end }}

  updateStrategy: SmartUpdate
  upgradeOptions:
    apply: disabled

  pmm:
    enabled: false
    image: percona/pmm-client:2.44.1
    serverHost: ""

  sharding:
    enabled: {{ .Values.sharding | default false }}
    balancer:
      enabled: true
    {{- if .Values.sharding }}
    configsvrReplSet:
      size: {{ .Values.shardingConfig.configServers }}
      resources: {{- include "cozy-lib.resources.defaultingSanitize" (list .Values.resourcesPreset .Values.resources $) | nindent 8 }}
      volumeSpec:
        persistentVolumeClaim:
          {{- with .Values.storageClass }}
          storageClassName: {{ . }}
          {{- end }}
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: {{ .Values.shardingConfig.configServerSize }}
      affinity:
        antiAffinityTopologyKey: kubernetes.io/hostname
      podDisruptionBudget:
        maxUnavailable: 1
    mongos:
      size: {{ .Values.shardingConfig.mongos }}
      resources: {{- include "cozy-lib.resources.defaultingSanitize" (list .Values.resourcesPreset .Values.resources $) | nindent 8 }}
      affinity:
        antiAffinityTopologyKey: kubernetes.io/hostname
      podDisruptionBudget:
        maxUnavailable: 1
      expose:
        exposeType: ClusterIP
    {{- end }}

  replsets:
    {{- if .Values.sharding }}
    {{- range .Values.shardingConfig.shards }}
    - name: {{ .name }}
      size: {{ .replicas }}
      resources: {{- include "cozy-lib.resources.defaultingSanitize" (list $.Values.resourcesPreset $.Values.resources $) | nindent 8 }}
      volumeSpec:
        persistentVolumeClaim:
          {{- with $.Values.storageClass }}
          storageClassName: {{ . }}
          {{- end }}
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: {{ .size }}
      affinity:
        antiAffinityTopologyKey: kubernetes.io/hostname
      podDisruptionBudget:
        maxUnavailable: 1
    {{- end }}
    {{- else }}
    - name: rs0
      size: {{ .Values.replicas }}
      resources: {{- include "cozy-lib.resources.defaultingSanitize" (list .Values.resourcesPreset .Values.resources $) | nindent 8 }}
      volumeSpec:
        persistentVolumeClaim:
          {{- with .Values.storageClass }}
          storageClassName: {{ . }}
          {{- end }}
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: {{ .Values.size }}
      affinity:
        antiAffinityTopologyKey: kubernetes.io/hostname
      podDisruptionBudget:
        maxUnavailable: 1
      expose:
        {{- if .Values.external }}
        enabled: true
        type: LoadBalancer
        externalTrafficPolicy: Local
        {{- else }}
        enabled: false
        {{- end }}
    {{- end }}

  {{- if .Values.users }}
  {{- /* Build a map of username -> list of roles from databases config */}}
  {{- $userRoles := dict }}
  {{- range $dbname, $db := .Values.databases }}
    {{- range $user := $db.roles.admin }}
      {{- $roles := index $userRoles $user | default list }}
      {{- $roles = append $roles (dict "name" "readWrite" "db" $dbname) }}
      {{- $roles = append $roles (dict "name" "dbAdmin" "db" $dbname) }}
      {{- $_ := set $userRoles $user $roles }}
    {{- end }}
    {{- range $user := $db.roles.readonly }}
      {{- $roles := index $userRoles $user | default list }}
      {{- $roles = append $roles (dict "name" "read" "db" $dbname) }}
      {{- $_ := set $userRoles $user $roles }}
    {{- end }}
  {{- end }}
  users:
    {{- range $username, $user := .Values.users }}
    {{- $roles := index $userRoles $username }}
    {{- if not $roles }}
    {{- fail (printf "user '%s' is not assigned to any database role in databases.*.roles" $username) }}
    {{- end }}
    - name: {{ $username }}
      db: admin
      passwordSecretRef:
        name: {{ $.Release.Name }}-user-{{ $username }}
        key: password
      roles:
        {{- range $roles }}
        - name: {{ .name }}
          db: {{ .db }}
        {{- end }}
    {{- end }}
  {{- end }}

  backup:
    enabled: {{ .Values.backup.enabled | default false }}
    image: percona/percona-backup-mongodb:2.11.0
    {{- if .Values.backup.enabled }}
    storages:
      s3-storage:
        type: s3
        s3:
          bucket: {{ .Values.backup.destinationPath | trimPrefix "s3://" | regexFind "^[^/]+" }}
          prefix: {{ .Values.backup.destinationPath | trimPrefix "s3://" | splitList "/" | rest | join "/" }}
          endpointUrl: {{ .Values.backup.endpointURL }}
          credentialsSecret: {{ .Release.Name }}-s3-creds
          insecureSkipTLSVerify: false
          forcePathStyle: true
    tasks:
      - name: daily-backup
        enabled: true
        schedule: {{ .Values.backup.schedule | quote }}
        keep: {{ .Values.backup.retentionPolicy | trimSuffix "d" | int }}
        storageName: s3-storage
        type: logical
        compressionType: gzip
    pitr:
      enabled: true
    {{- end }}
---
# WorkloadMonitor tracks data-bearing mongod pods only (not config servers or mongos routers)
# The selector filters by component=mongod, so we only count shard replicas
apiVersion: cozystack.io/v1alpha1
kind: WorkloadMonitor
metadata:
  name: {{ .Release.Name }}
  labels:
    workloads.cozystack.io/resource-preset: {{ .Values.resourcesPreset | quote }}
spec:
  {{- if .Values.sharding }}
  {{- $totalReplicas := 0 }}
  {{- range .Values.shardingConfig.shards }}
  {{- $totalReplicas = add $totalReplicas .replicas }}
  {{- end }}
  replicas: {{ $totalReplicas }}
  {{- else }}
  replicas: {{ .Values.replicas }}
  {{- end }}
  minReplicas: 1
  kind: mongodb
  type: mongodb
  selector:
    app.kubernetes.io/name: percona-server-mongodb
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: mongod
  version: {{ .Chart.Version }}
